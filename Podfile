# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'WNHL-App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire', '~> 5.2'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'SQLite.swift', '~> 0.13.0'
  pod 'ReachabilitySwift'
  pod 'Kingfisher' , '~> 7.0'

  # Pods for WNHL-App

  target 'WNHL-AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WNHL-AppUITests' do
    # Pods for testing
  end

end
